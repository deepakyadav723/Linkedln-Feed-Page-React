import _property from "lodash/property";

const name = _property("name");

const profilePic = _property("profilePic");

const highlights = _property("highlights");

const backgroundImage = _property("backgroundImage");

const location = _property("location");

const profileViewers = _property("profileViewers");

const postImpression = _property("postImpression");

// eslint-disable-next-line import/no-anonymous-default-export
export default {
  name,
  profilePic,
  highlights,
  backgroundImage,
  location,
  profileViewers,
  postImpression,
};
