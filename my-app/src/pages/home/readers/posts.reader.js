import _property from "lodash/property";

const name = _property("name");

const profilePic = _property("profilePic");

const highlights = _property("highlights");

const images = _property("images");

const description = _property("description");

const date = _property("date");

const likes = _property("likes");

const comments = _property("comments");

// eslint-disable-next-line import/no-anonymous-default-export
export default {
  name,
  profilePic,
  highlights,
  images,
  description,
  date,
  likes,
  comments,
};
